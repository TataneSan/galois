[package]
name = "galois"
version = "0.2.0"
edition = "2021"

[dependencies]
crossterm = "0.28"
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "benchmark_suite"
harness = false
